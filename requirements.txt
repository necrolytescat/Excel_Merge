# Runtime dependencies. FastAPI/Starlette are pinned together because the
# templates use compatibility behavior covered by the current test suite.
fastapi==0.115.14
starlette==0.46.2
uvicorn>=0.30,<1
jinja2>=3.1,<4
pydantic>=2,<3
openpyxl>=3.1,<4

# Test dependencies
pytest>=8,<9
httpx>=0.27,<1
