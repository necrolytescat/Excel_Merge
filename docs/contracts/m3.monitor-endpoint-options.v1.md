# m3.monitor-endpoint-options.v1 监控端点选项契约

> 状态：Phase 5 冻结
> Schema：app/schemas/monitor.py::MonitorEndpointOptionsPayload

仅返回已启用端点的 endpoint_id 与公开 label，按 label、endpoint_id 稳定排序。不得返回 URL、仓库 UUID、相对路径、Revision、凭据或其他分支身份字段。
