# M0.1 归档记录：SVN 基础配置与本地 Web 基座

## 归档信息

- 归档日期：2026-08-04
- 状态：已完成，可作为后续阶段基线
- 运行形态：本地 Web
- 正式 Provider：CLI
- SVN 权限：只读

## 已交付能力

- FastAPI + Uvicorn 服务；
- Jinja2 + 原生 HTML/CSS/JavaScript 页面；
- MockSVNProvider 和 CLISVNProvider；
- SVN 基础地址配置；
- 网页保存地址到 config/settings.json；
- 页面刷新后自动只读探测已保存地址；
- svn info 仓库信息展示；
- Windows SVN CLI 本地认证缓存；
- 非交互 CLI 调用和稳定错误码；
- 健康检查、配置 API 和基础连接 API；
- 目录、日志、内容只读 API 作为后续基础保留。

## 当前正式环境验证

- Provider：cli；
- SVN CLI：TortoiseSVN CLI 可用；
- KR/DEV svn info：读取成功；
- KR/DEV 当前 HEAD：26400；
- Source/table 和 Source/TableCsv 最早可读取 Revision：22805；
- 认证信息未进入配置、模型、日志或响应。

## 测试门禁

- 全量 pytest：103 passed；
- 配置保存只更新 svn.server_url；
- 带凭据 URL 被拒绝；
- Mock 与 API 契约测试通过；
- 未执行任何 svn commit、merge、update 或其他写操作。

## 明确延期

以下内容不属于本归档范围：

- KR/JP/TC/BT 与 Dev/Fix 端点注册表；
- 按日期日志页面和分页；
- 双端点选择、Diff 执行按钮和快照任务；
- CSV 语义 Diff、报告和 Merge；
- 本地 SVN 工作区支持；
- SVN 写回。

## 下一阶段入口

M1 以 docs/SVN-KR-DEV-STRUCTURE.md 为当前 KR/DEV 的结构参考，建立端点目录模型和双端点快照读取契约。JP、TC、BT 和 FIX 接入前必须分别读取并确认目录结构。