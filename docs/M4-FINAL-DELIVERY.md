# M4 表格计划对比最终交付

> 日期：2026-08-12  
> 状态：已完成

## 交付范围

- 左侧 M4 独立导航、长期计划 CRUD、归档恢复和运行历史。
- 基准 TABLE 目录、1～10 张表格、1～4 个目标分支、HEAD 与历史 Revision。
- 冻结运行、最多 40 个执行项、哈希短路、M2 单工作簿执行器复用。
- 多分支矩阵、目标页签、矩阵跳转、URL 恢复和原始 `m2.diff.v1` 明细。
- 取消、固定 Revision 失败项重试、重启恢复、全局工作簿并发门。
- 30 天明细清理、稳定 410、日志脱敏和 SVN 缓存边界。

## 验证证据

- 全量 pytest：`566 passed`。
- 阶段五重点测试：`17 passed`。
- JavaScript 语法检查：M4 页面脚本、结果页脚本和两个浏览器脚本全部通过。
- 真实 SVN：两侧冻结 r26579，10/10 完成，6 有差异、3 语义一致、1 完全相同、0 失败。
- 浏览器：1440px 与 390px 的矩阵、目标页签、工作簿跳转、M2 双栏字段和刷新恢复通过。
- 补丁检查：排除用户原有 `docs/ROADMAP.md` 行尾空格后通过。

## 已知限制

- 真实环境仅有两个启用分支，多目标上限由自动化测试验证。
- 过期明细不可恢复，只保留长期摘要；需要明细时重新运行计划。
- 当前未提供手工清理 API，也未自动创建 Git 提交。
